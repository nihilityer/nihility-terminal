syntax = "proto3";

package manipulate;

enum ManipulateType {
  DEFAULT_TYPE = 0;
  SPECIAL_TYPE = 1;
}

service Manipulate {
  rpc send_manipulate (ManipulateReq) returns (ManipulateResp) {}
}

message ManipulateReq {
  ManipulateType manipulate_type = 1;
  string command = 2;
}

message ManipulateResp {
  bool status = 1;
}